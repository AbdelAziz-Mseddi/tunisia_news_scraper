requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.10
python-dateutil>=2.8.2
playwright>=1.40.0

# Analysis module (sentiment). Region + urgency detection need nothing
# beyond the standard library. Install torch first (CPU-only build
# keeps it small and is all you need -- no GPU required):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install transformers
transformers>=4.35.0
